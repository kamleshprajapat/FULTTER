void main() {
  runApp(const Core());
}

class Core extends StatelessWidget {
  const Core({Key? key}); super (key: key);

  @override
  Widget build(BuildContext context){
    return Size(builder: ((context , orientatio,)));
  }
}
s